# Insights Page — Implementation Plan

> Spec: `docs/superpowers/specs/2026-05-22-insights-design.md`

**Goal:** Owner/admin Insights page from the More menu — 6 derived
analytics blocks.

---

### Task 1: `src/lib/insights.ts` — `computeInsights` + test

**Files:** Create `src/lib/insights.ts`, `tests/insights.test.ts`

- [ ] Types + `computeInsights(jobs, settings, today)` per the spec.
  - revenueTrend: 8 weeks, oldest→newest, zero-filled.
  - topServices/topSources/topCities: group, sum, sort.
  - repeat: from `deriveCustomerProfiles`.
  - unpaidAging: 4 buckets by day-age, `resolvePaymentStatus !== 'Paid'`.
- [ ] Test: week bucketing + zero-fill; ranking sort; repeat %;
  aging boundaries (7/8, 30/31, 60/61); mechanic profit; empty input.
- [ ] `npx tsx tests/insights.test.ts` → pass.

### Task 2: `TabId` + tab route

**Files:** Modify `src/types/index.ts`, `src/App.tsx`

- [ ] Add `'insights'` to the `TabId` union.
- [ ] In `App.tsx` `tabContent`: `if (tab === 'insights') return <Insights jobs={jobs} settings={settings} />;`
- [ ] Import `Insights`; add to the `tabContent` dep array if needed.
- [ ] `npx tsc --noEmit` → clean.

### Task 3: `src/pages/Insights.tsx`

**Files:** Create `src/pages/Insights.tsx`

- [ ] Page calls `computeInsights(jobs, settings, TODAY())`.
- [ ] Six cards: revenue-trend bar columns, three ranked-row lists
  (services/sources/cities), repeat-% stat, unpaid-aging table.
- [ ] Hand-rolled bars via existing CSS tokens; no new deps.
- [ ] `npm run build` → clean.

### Task 4: More-menu entry

**Files:** Modify the More-sheet component (found via App.tsx
`MoreSheet` / `onPick`)

- [ ] Add an "Insights" item (📊) that picks the `insights` tab.
- [ ] Gate to owner/admin — match how other owner-only More items
  are gated (role / permissions).
- [ ] `npm run build` → clean.

### Task 5: Verify + ship

- [ ] `npm run build` clean; `npm test` (43 logic) green;
  `npm run test:ui` (18 component) green.
- [ ] Commit + push.
